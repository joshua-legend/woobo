# CLAUDE.md — 우보브랜드샵 랜딩페이지

## 프로젝트
- 우보브랜드샵(Woobo) 브랜드 랜딩페이지 1탄. Next.js, 스크롤 기반 롱폼.
- 정체성: 우보인터내셔날(주) / Woobo = **Blum 한국 독점 에이전트(sole agent)** · 정품의 공식 통로 · 멀티브랜드 수입(Blum·AGOFORM·Peka 등) · 전국 쇼룸. (**Blum 본사 아님**)
- 주요 CTA: **전국 쇼룸 방문 예약**.
- 가안 A·B·C 3종(매니페스토 / 문제해결 / 기원서사)이 마이그레이션됨. 디벨롭 = 섹션별 인터랙션 + 문구 + 이미지.

## 작업 원칙 (항상 지킴)
- **섹션 하나씩 끝까지** 완성(인터랙션+문구+이미지) 후 다음 섹션. 여러 섹션 동시에 찔끔 금지.
- 코드 짜기 전에 **plan mode로 계획 → 승인 후 실행.**
- 컨텍스트엔 **한 번에 한 섹션만.**
- 섹션 끝나면 **git commit** (롤백 단위).
- 작업 끝에 **`WORKLOG.md`에 한 칸 추가.**
- 코딩 태도 4원칙:
  1. **생각 먼저** — 모르면 가정하지 말고 물어라.
  2. **단순함 우선** — 필요한 최소만. 안 시킨 기능·추상화 추가 금지.
  3. **수술적 변경** — 요청 범위만. 무관한 파일·리팩터링 건드리지 마.
  4. **목표 기반** — 성공 기준(돌아가는 화면)으로 검증하고 끝내라.

## 규칙 1 — 모션 (★ 일관성 핵심)
- 토큰:
  - `--ease: cubic-bezier(.16,1,.3,1)` = **소프트클로즈. 모든 transition/reveal/hover 기본값.**
  - `--ease-tip: cubic-bezier(.34,1.56,.64,1)` = TIP-ON 팝(톡 튀는 요소만).
  - '쾅' 예외: `cubic-bezier(.72,0,1,1)` 0.13s — **문제 섹션에서 단 1회만** 소프트클로즈를 깸.
- 스크롤 스크럽 = **rAF + lerp 보간**(점프 금지). **공용 훅 1개**로 통일.
- reveal 2종: `.up`(아래→위 fade+24px), `.mask`(헤드라인 라인마스크, 줄당 70ms stagger).
- **reduced-motion**: 전부 최종 정적 상태로 graceful degrade. 기본은 모션 ON.

## 규칙 2 — 카피 가드레일
- **금지:** "우리가 곧 Blum", "Blum 코리아 / 본사".
- **사용 OK:** 우보인터내셔날(주) / 우보브랜드샵 / Woobo / Blum 한국 독점 에이전트(sole agent) / 정품의 공식 통로 / 전국 쇼룸 / 자체 가구 생산(김포) / 멀티브랜드 수입.
- **정품·"유사품 주의" 메시지 적극 사용**(자사 실제 입장). 단 경쟁·병행수입 직접 비교는 검증된 사실 범위에서만.
- 수치는 **"(Blum) 자체 시험 기준"** 명시. **"평생보증" 단정 금지.** 하중 = **"최대 약 70kg급"**.
- 미확정(재고·납기·우보 연혁·법적 등급 표기·A/S 범위)은 **placeholder** 표기, 임의 단정 금지.
- 톤: 제작자·고객에게 말 거는 1인칭 + 프리미엄. **가격·할인 전면화 금지.**

## 규칙 3 — 이미지
- 팔레트 일치: 페이퍼 `#f4f1ec` / 차콜 `#16140f` / 잉크 `#1c1a17` / 오렌지 `#ff671f`(**액센트 전용, 면적으로 쓰지 마**) / 라인 `#e2ddd4`.
- 타이포: Pretendard(헤드) / IBM Plex Sans KR(본문) / IBM Plex Mono(라벨), `word-break: keep-all`.
- 이미지 슬롯은 매니페스트로 관리: `{ id, section, 비율, intent, prompt, alt }` (파일: `image-manifest.md`).
- **AI 생성 = 가안·무드까지만.** 실제 Blum 제품샷은 출시 전 실물로 교체 (**AI 합성 제품을 '정품'이라 쓰지 말 것** — 정품 메시지와 충돌).
- placeholder는 빗금 + 모노 캡션으로 자리·비율만 잡고, src만 갈아끼우면 되게.

## 파일
- 작업 로그: `WORKLOG.md` (매 세션 append)
- 이미지 슬롯: `image-manifest.md`

---
*대괄호·세부 수치는 프로젝트 확정값으로 교체. 규칙이 절차로 커지면 `.claude/skills/`로 분리.*
